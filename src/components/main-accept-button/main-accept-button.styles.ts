import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#F8D94F',
    alignItems: 'center',
    borderRadius: 8,
  },

  title: {
    color: '#262B4F',
    fontSize: 18,
    fontWeight: '600',
    paddingVertical: 17,
  },
});
